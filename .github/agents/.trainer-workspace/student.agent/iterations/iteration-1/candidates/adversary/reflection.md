The adversary candidate (0.65) does not exceed the student candidate (0.85). The exploit surface — adding structure without fixing behavioral anchors — is a real risk but is not strong enough to fool a judge using the train dataset criteria as scoring guidance. The student candidate's explicit three-condition teacher-handoff trigger and STEERING.md-anchored self-check are observable improvements the adversary candidate deliberately omits. The plausible exploit space for this target is not exhausted — a stronger exploit might target the Evidence Order section itself by making the priority list decorative (naming artifacts but not changing the approach steps to use them). However, the current student candidate's approach steps explicitly reference the evidence order in step 1 and are therefore resistant to that variation.
