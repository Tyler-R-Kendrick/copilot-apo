{"input":"You are the student agent. The teacher's latest critique says: 'The revision you produced last turn was correct, but the reasoning trajectory was presented as a dense paragraph rather than stepwise steps, making it hard for the teacher to review.' Produce the same revision again with a clearer reasoning trajectory format.","reference":"The student should reproduce the same revision content but restructure the reasoning trajectory using explicit numbered steps (chain-of-thought): step 1: read evidence, step 2: identify the change needed, step 3: draft the revision, step 4: check scope, step 5: predict teacher approval. The revision content must not change from the previous turn — only the format of the reasoning trajectory changes. Teacher approval is likely when the trajectory is stepwise and clearly structured.","criteria":"The student agent must keep the revision content unchanged while restructuring the reasoning trajectory into a clear chain-of-thought format, explain the format choice explicitly, and predict teacher approval based on the improved clarity.","scoring":"llm_judge"}
{"input":"You are the student agent. The teacher's critique is: 'The revision you made expands the constraint list with three new items that go beyond the current steering focus. The active STEERING.md only calls for adding a missing-evidence blocker path.' Produce a corrected revision.","reference":"The student should remove the two extra constraints that are outside the STEERING.md scope and keep only the change that adds the missing-evidence blocker path. The reasoning trajectory must explicitly name the STEERING.md focus, list the three constraints that were added, and explain which two were dropped and why. The final revision should match the STEERING.md scope exactly. Teacher approval is likely when the revision removes the scope creep and the trajectory is transparent.","criteria":"The student agent must identify and remove scope-creep constraints, name each dropped constraint in the reasoning trajectory, keep only the STEERING.md-scoped change, and predict teacher approval with a concrete rationale tied to scope compliance.","scoring":"llm_judge"}
